# Server Configuration
PORT=3000

# ============================================
# Optimizely Graph Configuration (Recommended)
# ============================================
# Use these variables for Optimizely Graph CMS

# Optimizely Graph Gateway URL
OPTIMIZELY_GRAPH_GATEWAY=https://cg.optimizely.com

# Optimizely Graph Single Key (for public access)
OPTIMIZELY_GRAPH_SINGLE_KEY=YOUR-KEY-HERE

# Optimizely Graph App Key (for authenticated access)
OPTIMIZELY_GRAPH_APP_KEY=YOUR-KEY-HERE

# Optimizely Graph Secret (for server-side mutations - optional for read-only)
OPTIMIZELY_GRAPH_SECRET=YOUR-SECRET-HERE

# Enable external preview mode (allows preview without CMS editor token)
EXTERNAL_PREVIEW_ENABLED=true

# ============================================
# Generic GraphQL Configuration (Alternative)
# ============================================
# Use these if you're NOT using Optimizely Graph

# Generic GraphQL endpoint
# GRAPHQL_ENDPOINT=http://localhost:4000/graphql

# Generic API key (sent as Bearer token)
# GRAPHQL_API_KEY=your-api-key-here

# ============================================
# Notes:
# ============================================
# - If OPTIMIZELY_GRAPH_GATEWAY and OPTIMIZELY_GRAPH_SINGLE_KEY are set,
#   they take precedence over GRAPHQL_ENDPOINT
# - You only need either Optimizely Graph OR Generic GraphQL config, not both
# - OPTIMIZELY_GRAPH_SECRET is optional for read-only operations
